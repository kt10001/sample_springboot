<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <property name="SERVER_NAME" value="robot"/>
    <property name="LOG_HOME" value="${SPRING_LOG_PATH:-./log}" />
    <property name="MAX_HISTORY" value="10" />
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
<!--            <pattern>[${SERVER_NAME}]%d{yyyy-MM-dd HH:mm:ss.SSS}[%-5level][%thread][traceId:%X{traceId}][%file:%line] -%msg%n</pattern>-->
            <pattern>%msg%n</pattern>
        </encoder>
    </appender>
    <!--全量日志输入文件存储-->
    <appender name="FILE_ALL" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <encoder>
<!--            <pattern>[${SERVER_NAME}]%d{yyyy-MM-dd HH:mm:ss.SSS}[%-5level][%thread][traceId:%X{traceId}][%file:%line] -%msg%n</pattern>-->
            <pattern>%msg%n</pattern>
        </encoder>
        <file>${LOG_HOME}/${SERVER_NAME}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/full/%d{yyyy-MM-dd}/${SERVER_NAME}.%i.log</fileNamePattern>
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <MaxFileSize>100MB</MaxFileSize>
            <totalSizeCap>10GB</totalSizeCap>
        </rollingPolicy>
    </appender>
    <appender name="ERROR" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
<!--            <pattern>[${SERVER_NAME}]%d{yyyy-MM-dd HH:mm:ss.SSS}[%-5level][%thread][traceId:%X{traceId}][%file:%line] -%msg%n</pattern>-->
            <pattern>%msg%n</pattern>
        </encoder>
        <file>${LOG_HOME}/${SERVER_NAME}-error.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/error/%d{yyyy-MM-dd}/${SERVER_NAME}-error.%i.log</fileNamePattern>
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <MaxFileSize>100MB</MaxFileSize>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>
    <root level="INFO">
        <appender-ref ref="STDOUT" />
        <appender-ref ref="FILE_ALL"/>
        <appender-ref ref="ERROR" />
    </root>
</configuration>